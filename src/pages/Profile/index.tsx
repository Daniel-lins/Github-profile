import { Container } from "./styles";
import { ViewProfile } from "../../components/ViewProfile";

export const Profile = () => {
  return (
    <Container>
      <ViewProfile />
    </Container>
  );
};
